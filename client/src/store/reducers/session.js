const initialState = {
  user: {
    //TODO: It will be filled when user logged in
    id: '5d2bc2680ef58a2f8a24d438',
  }
};

const session = (state = initialState) => {
  return state;
};

export default session;